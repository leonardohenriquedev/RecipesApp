// import React from 'react';
// import userEvent from '@testing-library/user-event';
// import renderWithRouter from './renderWithRouter';
// import App from '../App';

describe(`Desenvolva a tela de maneira que contenha uma imagem da receita, seu titulo, 
    sua categoria ou se a bebida é alcoólica ou não uma lista de ingredientes 
    com suas respectivas quantidades e suas instruções - Req 47`, () => {
  test('Verifica elementos de uma receita de comida', () => {
    // const {} = renderWithRouter(<App />);
  });

  test('Verifica elementos de uma receita de comida', () => {
    // const {} = renderWithRouter(<App />);
  });
});

describe(`Desenvolva um checkbox para cada item 
    da lista de ingredientes- Req 48`, () => {
  test('todos os ingredientes de uma receita de comida possuem um checkbox', () => {
    // const {} = renderWithRouter(<App />);
  });
  test('todos os ingredientes de uma receita de comida possuem um checkbox', () => {
    // const {} = renderWithRouter(<App />);
  });
});

describe(`Implemente uma lógica que, ao clicar no checkbox de um 
    ingrediente, o nome dele deve ser "riscado" da lista - Req 49`, () => {
  test('verifica se é possível marcar todos os passos da receita de comida', () => {
    // const {} = renderWithRouter(<App />);
  });
  test('verifica se é possível marcar todos os passos da receita de bebida', () => {
    // const {} = renderWithRouter(<App />);
  });
});

describe(`Salve o estado do progresso, que deve ser mantido caso a pessoa 
    atualize a página ou volte para a mesma receita- Req 50`, () => {
  test('Salva o progresso de uma receita de comida em andamento', () => {
    // const {} = renderWithRouter(<App />);
  });
  test('Salva o progresso de uma receita de bebida em andamento', () => {
    // const {} = renderWithRouter(<App />);
  });
});
